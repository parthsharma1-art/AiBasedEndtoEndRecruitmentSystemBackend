spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: aibasedendtoendsystem
    redis:
      url: ${REDIS_URL}
  cache:
    type: redis

  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 100MB

server:
  port: 8081
  servlet:
    context-path: /api
app:
  auth:
    otp:
      ttl-minutes: 10
      # Single Render dyno: OTP works if Redis is down. Set false when REDIS_URL is correct (rediss://) or you scale horizontally.
      memory-fallback-when-redis-unavailable: ${OTP_MEMORY_FALLBACK:true}
  oauth2:
    client:
      registration:
        google:
          provider: google
          client-id: 933277047426-lc7eeb96h30umrm6qfoncp6l27026s07.apps.googleusercontent.com
          client-secret: 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
          redirectUriForRecruiter: https://aibasedendtoendrecruitmentsystembackend.onrender.com/api/recruiter/google/login
          redirectUriForCandidate: https://aibasedendtoendrecruitmentsystembackend.onrender.com/api/candidate/google/login

app.config:
  url:
    baseUrl: https://aibasedendtoendrecruitmentsystembackend.onrender.com
    frontEndUrl: https://ai-based-endto-end-recruitment-syst.vercel.app
    hostFrontEndUrl: https://ai-based-endto-end-recruitment-syst.vercel.app


brevo:
  api:
    key: ${BREVO_API_KEY}
  sender:
    email: parthsharma7817@gmail.com
  receiver:
    email: ai.recruitment.platform@gmail.com

cron:
  job:
    time: "${CRON_JOB_TIME:0 0 */14 * * *}"
    shortlistEvaluation:
      time: "${CRON_SHORTLIST_EVALUATION_TIME:0 */8 * * * *}"
    aiServiceKeepAlive:
      time: "${CRON_AI_SERVICE_KEEP_ALIVE_TIME:0 */4 * * * *}"


ai-service:
  base-url: ${AI_SERVICE_BASE_URL:https://aibasedendtoendrecruitmentsystemaiservic-xa2o.onrender.com}

shortlist:
  evaluate:
    url: ${ai-service.base-url}/shortlist/evaluate
    batchUrl: ${ai-service.base-url}/shortlist/evaluate-batch

ai:
  test:
    generate:
      url: ${ai-service.base-url}/test/generate


razorpay:
  key-id: ${RAZORPAY_KEY_ID}
  key-secret: ${RAZORPAY_KEY_SECRET}
  webhook-secret: ${RAZORPAY_WEBHOOK_SECRET}